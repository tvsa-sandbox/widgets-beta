import styled, { css } from "styled-components";

const BrandTitleStyles = styled.section(
    ({ theme }) => css`
    width: 100%;
    display: flex;
    justify-content: center;
    cursor: pointer;
`,
);


export default BrandTitleStyles;
